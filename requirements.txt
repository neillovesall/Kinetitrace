# KineTrace V7 — Python dependencies

# Core web stack
flask>=3.0.0
flask-socketio>=5.3.6
python-socketio>=5.10.0     # for simulate_serial.py --mode ws
python-engineio>=4.8.0
eventlet>=0.35.2             # async mode (alternative: gevent)

# Math / signal
numpy>=1.26.0
scipy>=1.12.0
numba>=0.59.0                # JIT for DTW; falls back gracefully if absent

# Hardware
pyserial>=3.5                # optional; required for --mode serial / hardware

# Simulator
requests>=2.31.0